# Veřejné proměnné frontendu (Vite). NIKDY sem nedávej serverové secrety.
# Zkopíruj do .env a vyplň podle potřeby.

# Základ URL budoucího API (vystaveno-api).
# Prázdné = mock datová vrstva přes localStorage (výchozí pro MVP).
VITE_API_URL=
