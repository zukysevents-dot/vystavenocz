# CLAUDE.md

Tradeoff: Tato pravidla preferují opatrnost před rychlostí. U triviálních úkolů použij selský rozum.

## 0. O projektu

**Vystaveno.cz** — migrace ze staré React appky na **Vue 3**, zatím frontend-only MVP.

- **Stack:** Vue 3 (`<script setup>` SFC) + Vite + TypeScript. Package manager **npm**.
- **Komunikace:** s uživatelem **česky**, kód a identifikátory **anglicky**.
- **Příkazy:**
  - `npm run dev` — vývojový server (localhost)
  - `npm run build` — produkční build včetně typecheck (`vue-tsc`)
  - `npm run lint` — ESLint
  - `npm run format` — Prettier
- **Repo:** [github.com/zukysevents-dot/vystavenocz](https://github.com/zukysevents-dot/vystavenocz) — produkční větev `main` = **Vue app**. Stará React appka je zazálohovaná ve větvi `legacy-react` (jen reference, NEvracet do working tree). Vyvíjí se na feature větvích.
- **Plán práce:** GitHub Project #1 — 58 tasků, fáze F0–F8.
- **Produktová roadmapa:** `docs/product/modular-business-roadmap.md` — modulární Vystaveno, gastro priority a konkurenční benchmark.
- **Uživatelský manuál:** `docs/product/vystaveno-user-guide.md` je centrální krátký průvodce všemi obory; detailní návody jsou `gastro-user-manual.md`, `sluzby-zakazky-manual.md` a `opakovane-faktury-manual.md`. Při uživatelsky viditelné změně aktualizuj příslušný návod. `/app/pruvodce` v aplikaci zobrazuje podle role a zapnutých modulů krátké kroky z `src/lib/help.ts`; drž ho v souladu s navigací a návody. `docs/product/interni-test-manual.md` je stručný průvodce pro interní test na VPS staging (gastro start, sklad/inventura, uzávěrka, integrace, podpisy) s tabulkou **co je ostré vs. jen připraveno k napojení** — drž ji pravdivou.
- **Continuation prompt:** `docs/agent-workflow/claude-gastro-pipeline-prompt.md` — copy-paste prompt pro Claude Code, aby mohl autonomně pokračovat v gastro roadmape přes architect → implement → QA → review → docs → PR workflow.
- **Deploy:** produkce běží pouze na VPS přes Docker stack v tomto repu (`docker-compose*.yml`, `nginx.conf`, `Caddyfile`); deploy je pull-based a nespouští se automaticky po merge. VPS `.env` musí obsahovat `PAYMENTS_PORTAL_BASE_URL` (veřejná URL aplikace, typicky `https://$DOMAIN`), jinak online checkout faktury nemá kam vrátit zákazníka. Po deployi projdi `docs/deploy-smoke-checklist.md` (health/live, health/ready, `/api/v1/ping`, přihlášení, moduly, POS prodej, restaurace/stůl, kuchyně, uzávěrka, sklad/inventura/zrcadlo, Pohoda XML export, tiskový agent, platební provider settings + vault, podpisy provider settings + vault + odeslání, veřejné/QR menu a audit). Před deployem ověř zálohu DB; migrace jsou forward-only.
- **Public web:** aktuální landing page na `/` musí při VPS deployi zůstat zachovaná. Aplikace běží pod `/app`, API pod `/api`; deploy smoke kontroluje `/` zvlášť, aby se homepage omylem nepřepsala.
- **Produkční spolehlivost V1:** `ops/vps-deploy.sh` je podporovaná deploy cesta: sdílí lock se zálohou, spustí konzistentní `PostgreSQL + api_files` snapshot a čeká na health. `ops/vps-backup.sh` publikuje balík atomicky až po ověření, `ops/vps-verify-backup.sh` obnovuje pouze v izolovaném PostgreSQL a `ops/vps-health-check.sh` hlídá služby/endpointy/stáří backupu/disk. Instalace, retence, off-site hranice a recovery jsou v `docs/vps-reliability.md`; `.ops.env` je server-only, gitignored a mode 600. `INTEGRATIONS_SECRET_ENCRYPTION_KEY` nikdy neměň jako rychlou opravu.
- **Ceník:** `src/lib/pricing.ts` a `IndustryPackages.vue` popisují jen skutečně nasazené workflow a u hotových nástaveb zobrazují „V aplikaci". Nezaměňuj integrační foundation za ostrý provoz: klientská zóna nepřijímá online platbu bez provider adapteru, cashflow jen připraví e-mailovou upomínku a BankID/platební provideri zůstávají „Připraveno k napojení", dokud nejsou aktivní.
- **Předání zakázky:** `handover.items` je neměnný serverový snapshot práce a materiálu s `kind` `work`/`material`. `useJobs.get()` ho vždy normalizuje na pole, protože neúplná starší API odpověď nesmí shodit `ZakazkaDetailPage.vue`.
- **Staging demo data:** backend `vystaveno-api` má explicitní CLI helper `dotnet Vystaveno.Api.dll seed-demo`, který se spouští jednorázově přes Docker exec s `Seed__AllowDemo=true` (viz `docs/deploy-smoke-checklist.md`). Naplní demo firmu `Vystaveno Demo Gastro` pro interní smoke test; login je `demo@vystaveno.cz` / `DemoGastro.2026`. Demo **simuluje cca rok provozu napříč všemi moduly** (roční historie prodejů přes 3 pobočky, uzavřené dny/Z-reporty za rok + měsíční exporty, fakturace za rok se stavy zaplaceno/po splatnosti/vystaveno, publikované směny + odpracovaná docházka, rezervace ve víc stavech, šablona opakovaných faktur s historií běhů), aby reporty/přehledy/uzávěrky nebyly prázdné. Backend seeder žije v `DemoDataSeeder*.cs`; roční historie prodejů se backdatuje jedním hromadným UPDATE (`unnest`), historické Z-reporty se dopočítají z denní agregace prodejů. **Roční data vzniknou jen při PRVNÍM seedu firmy** — seeder je idempotentní přeskočením, takže na už existující demo firmě opakovaný běh jen dorovná chybějící moduly; pro bohatší demo se stávající demo firma musí nejdřív smazat a naseedovat znovu (izolovaná ne-produkční data). Sklad zůstává na snapshotu (roční skladové pohyby vědomě mimo scope). Není to běžná produkční funkce, nespouští se při startu aplikace a nespouštěj ho na reálné produkční databázi.
- **Modulární runtime základ:** `/me` vrací `modules` + `features`; frontend je drží v `src/stores/auth.ts` a app shell filtruje navigaci/routy přes `src/lib/modules.ts`. Backend ukládá tenant-selected moduly v `company_modules`; onboarding ukládá první balíček podle typu podnikání, ukazuje profilový checklist `setupSteps` a po uložení pošle firmu na první doporučený krok (Gastro začíná v `Pobočky`). `Nastavení firmy` umožňuje pozdější změnu modulů přes `/company/modules`. Modul `core` je povinný a normalizace ho vždy doplní. **Stránky nesmí shodit celý view, když tenant nemá modul k části dat:** `Přehled`/`DashboardPage.vue` volá fakturační část (metriky faktur, graf tržeb, poslední faktury/klienti + tlačítka `Nový klient`/`Nová faktura`) jen při `auth.hasModule('invoicing')` — bez modulu backend vrací 403 a dřív to shodilo celý Přehled jako „Server je momentálně nedostupný"; teď se fakturační část přeskočí a skryje, POS/gastro provoz zůstává. Vzor gatování je `computed(() => auth.hasModule('X'))` (jako `loyaltyEnabled` v Pokladně/Restauraci). E2E `e2e/dashboard-modules.spec.ts`. Ze stejného důvodu je stránka i nav `Klienti` gate-ovaná modulem `invoicing` (ne `core`): klient je fakturační entita a backend `/clients` gatuje `Permissions.Invoices.Read → ProductModules.Invoicing`, takže bez modulu vracel 403 a `KlientiPage` ukazovala stejné zavádějící „Server je momentálně nedostupný". Tenant bez invoicing `Klienti` v navigaci nevidí a přímý přechod na `/app/klienti` route guard přesměruje na Přehled. E2E `e2e/klienti-modules.spec.ts`.
- **Fakturace — typy dokladu (Invoicing V2):** modul `invoicing` umí kromě běžné faktury i **zálohovou (proforma)** a **dobropis**. `DocumentType = 'invoice' | 'proforma' | 'credit_note'` + `Invoice.parentInvoiceId` (`src/lib/types.ts`). Editor `FakturyEditorPage.vue` vybírá typ Faktura/Zálohová (dobropis se v editoru NETVOŘÍ ani needituje); `FakturyPage.vue` má filtr + badge dle typu a akce **„Vystavit dobropis"** (na vystavené/uhrazené faktuře) a **„Převést na fakturu"** (na proformě) přes `useInvoices.creditNote(id)` / `convertToInvoice(id)`. **Dobropis vytváří backend** (`POST /invoices/{id}/credit-note`) se zápornými částkami/DPH — frontend NEfabrikuje záporné položky ani nepočítá DPH, `quantity` zůstává KLADNÉ (backend validator `> 0`), FE jen zobrazí zápornou serverovou odpověď; mock (`creditNoteItems`) je jen stand-in, který otočí znaménko už spočítaných částek. **Dobropis vzniká rovnou vystavený** (Issued, očíslovaný z vlastní řady) a **needituje se** — editor má guard proti přímé URL (jinak by přepočítal zápornou částku na kladnou). **Proforma** (nedaňový doklad) je vyloučená z DPH, pohledávek, obratu, dashboardu i účetního exportu; daňový doklad vzniká konverzí (`POST /invoices/{id}/convert-to-invoice`). **Dobropis** (daňový doklad) naopak v DPH (`dph.ts`) i účetním exportu (`UctarnaPage.vue`, Pohoda `DocumentType` 2) **nettuje záporně**; pohledávek (`cashflow.ts`) ani věrnostního obratu (`loyalty.ts`) se netýká — ty berou jen `documentType === 'invoice'`. A11y invariant editoru: každý `Input`/Select trigger má spárovaný `Label` + stabilní `id`, ikonový přepínač náhledu má dynamický `aria-label`; `e2e/a11y.spec.ts` to hlídá v serious/critical axe gate. Backend kontrakt (staví Codex) je `docs/backend/invoicing-v2.md`. E2E `e2e/faktura-doklady.spec.ts`, unit `src/lib/invoice.spec.ts`. **FE↔BE mapovací adapter (jen API režim):** invoicing FE byl původně psaný proti mock tvaru a nikdy nebyl namapován na backend; `src/lib/invoice-api.ts` překlápí backend DTO ↔ FE `Invoice` (`invoiceFromApi`/`invoiceToCreateRequest`/`invoiceToUpdateRequest`): enum casing `Draft|Issued|Paid|Cancelled|Sent|Overdue|Archived`→FE lowercase a `Invoice|Proforma|CreditNote`→`invoice|proforma|credit_note` (idempotentně), adresy `postalCode`↔`zip`, banka `bic`↔`swift`, `supplierName`→`companyName`, `isVatPayer`→`vatMode`, řádky `lineBase`→`lineSubtotal`; **peníze/DPH FE nepočítá, jen přemapuje serverová čísla — znaménka dobropisu se zachovají**. Aplikuje se VÝHRADNĚ v API režimu ve `useInvoices` (mock/localStorage vrstva beze změny). `useInvoices.pay` volá správně `POST /invoices/{id}/mark-paid` s `{ idempotencyKey }`, NE neexistující `/pay`. List `GET /invoices` vrací jen **summary bez řádků** (→ `items: []`), takže detail/editace táhne `useInvoices.get(id)` = `GET /invoices/{id}` (plný doklad). **Fakturace dokončená v API režimu (GAP 1+2):** editace řádků rozpracovaného konceptu se po PUT hlavičky synchronizuje přes `/items` endpointy — `useInvoices.update()` v API režimu spočítá čistý plán `diffInvoiceLines(serverIds, editorItems)` (v `invoice-api.ts`, unit-testovaný) a provede ho v **bezpečném pořadí**: `PUT /items/{id}` úpravy → `POST /items` nové **PŘED** mazáním (ať draft nikdy nespadne na 0 řádků) → `DELETE /items/{id}` → `PUT /items/reorder` finální množinou server-id v pořadí editoru; nový řádek nemá server id předem → klientské uuid se mapuje na server id z POST odpovědi (reorder jen když se složí celá množina). **Přehled DPH** (`DphPage.vue`) bere v API režimu čísla z `GET /invoices/vat-summary?from=&to=` přes `useInvoices.vatSummary(from,to)` (tvar `VatSummary`, server nettuje dobropis a vylučuje proformu); období 'YYYY-MM' → from=1./to=poslední den měsíce, 'all' → bez rozsahu; nabídka období z `issueDate` summary listu. **Mock režim DphPage** dál počítá client-side přes `dph.ts`. **Účtárna** (`UctarnaPage.vue`): CSV vystačí s hlavičkovými součty (`list-summary` teď nese `subtotal`/`vatTotal`), ISDOC potřebuje řádky → v API režimu je `exportIsdoc` async, dotáhne plný detail `get(id)` a teprve pak `downloadIsdoc` (mock má řádky → beze změny). **Peníze/DPH FE v API režimu nikdy nepočítá — bere serverová čísla, znaménka dobropisu zachová.** Unit `src/lib/invoice-api.spec.ts` (`diffInvoiceLines`), e2e `e2e/dph-api.spec.ts` + `e2e/uctarna-api.spec.ts`.
- **Workforce V2 — plánovač směn, docházka, payroll (modul `attendance`):** rozšíření existujícího modulu `attendance`, **žádný nový modul ani permission**. Konsolidace dvou dřívějších systémů směn na FK-based `Shift` (`/shifts`) obohacený o `status` (`Draft|Published`), `position` (free text) a `hourlyRateOverride`; **mzdový domov = `Employee.hourlyRate`** (+`position`), efektivní sazba `override ?? employee ?? 0` (shodně FE i BE). Starý free-text `/staff-shifts` zůstává na backendu netknutý, ale **FE ho už nepoužívá** (deprecated) — žádná destruktivní migrace. Kontrakt `docs/backend/workforce-v2.md`. `SmenyPage.vue` je **týdenní plánovač** (mřížka zaměstnanci×dny per pobočka, navigace týdnů, Draft čárkovaně / Published plně, `Publikovat týden` → audit `ShiftsPublished`, šablony `ShiftTemplatesDialog.vue` + „Použít", `Export plánu` CSV z plánovaného nákladu, blok provize zachován, desktop mřížka + mobil seznam + sticky lišta). Composables `useShifts`/`useShiftTemplates` jsou **mock-capable** (localStorage větev stejného tvaru jako API); `stores/shifts.ts` smazán (osiřel po přepisu). `src/lib/shifts.ts` počítá hodiny z UTC, `effectiveRate`, `buildPayrollPreview`, `totals`; `calculateCommission` beze změny (unit `src/lib/shifts.spec.ts`). `DochazkaPage.vue`: zaměstnanec má `position`+`hourlyRate` (jen manažer je vidí/mění), přehled ukazuje `wageCost` + `Export mezd CSV`; manažerské taby `Opravy` (`AttendanceCorrectionsPanel.vue` → `PUT /attendance/records/{id}`, audit `AttendanceCorrected`, **location guard** — manažer opravuje jen svou pobočku) a `Výjimky` (`AttendanceExceptionsPanel.vue` → `GET /attendance/exceptions`, `AttendanceExceptionKind` = `MissingClockOut|Overtime|PlanVsActualVariance`, prahy MVP konstanty) — API-only, gate `computed(() => ['Owner','Admin','Manager'].includes(auth.role))`. Oprávnění: `attendance.manage` = plán/publikace/korekce/mzda; **ne-manažer vidí jen `Published` směny**; **wage privacy** — `Employee.hourlyRate` je `null` bez `attendance.manage`; Manager je location-scoped. Backend (`vystaveno-api`, jeden PR): entity delty + migrace `WorkforceV2ShiftPlanning` (backfill existujících směn na `Published`), `/shifts` (+`/shifts/publish`), `/shift-templates` CRUD, `/employees` (+pozice/sazba), `/attendance/summary|export` (+mzda), `/attendance/exceptions`, location scoping přes `LocationScope`, integrační testy vč. tenant isolation + role/location authz. Demo data v mock seedu (`src/lib/seed.ts`) i v `docs/product/gastro-user-manual.md`. E2E `e2e/smeny-planner.spec.ts`. Nav „Směny" i route `/app/smeny` zůstávají pod modulem `attendance` (název stránky „Plán směn").
- **Services & Jobs V2 — řemeslná vertikála (modul `jobs`):** rozšíření modulu `jobs`, **žádný nový modul**. Katalog služeb → nabídka → zakázka → pracovní list (práce + materiál ze skladu) → předání → faktura. Kontrakt `docs/backend/services-jobs-v2.md`. **DPH/součty počítá VÝHRADNĚ backend** (řádky nesou serverem spočtené `lineNet/lineVat/lineTotal`, detail `subtotal/vatTotal/total`) — FE je jen zobrazuje. `ZakazkaDetailPage.vue` proto ty serverové částky **skutečně čte** přes `jobLineTotal`/`resolveJobTotals`/`jobIsTaxed` (`src/lib/jobs.ts`); dřív worksheet přepočítával DPH z `companyStore.vatMode`, jenže backend `/company` vatMode NEVRACÍ (je to jen localStorage overlay, default `non_payer`), takže plátci DPH ukazoval jen NETTO místo částky s DPH (faktura pak byla vyšší). `computeJobTotals`/`calcLine` s `vatPayer` zůstávají jen jako mock/živý náhled fallback (server čísla chybí → dopočítá se). `Job` typ má ploché `subtotal?/vatTotal?/total?` (jako faktura/nabídka), `JobWorkItem`/`JobMaterialItem` volitelné `lineNet?/lineVat?/lineTotal?`. Nový katalog `ServiceItem` (`useServiceItems`, `/api/v1/service-items`, `jobs.read/manage`, `ServiceCatalogPage.vue` na `/app/cenik-sluzeb`, nav `hiddenForRoles: ['Employee','Accountant']`). `Job` V2 rozšiřuje entitu (staré agregát sloupce ponechány, default 0, nepoužívají se): `number` (`ZAK-{yyyy}-{seq}`), `clientId?` FK, `siteAddress`, `scheduledAt`, `assignedEmployeeId?`, `priority` (`Low|Normal|High|Urgent`), `locationId?`, `sourceQuoteId?`, `invoiceId?`; `JobStatus` `scheduled|in_progress|waiting|done|cancelled` (guarded, přes `POST /jobs/{id}/status`). **`ZakazkaDetailPage.vue` `/app/zakazky/:id`**: pracovní list (`JobWorkItem` z katalogu/volně; `JobMaterialItem` s product-pickerem z Naskladnění → `POST/DELETE /jobs/{id}/material-items` **odečítá/vrací sklad** na backendu, movement + `RelatedJobId` + audit `JobMaterialConsumed/Returned`), checklist, časová osa (`JobEvent`), předání (`JobHandover` + `POST /jobs/{id}/handover/sign` seam přes `SigningEnvelope`, jen modul `verified_signing`, NE ostrý BankID), **„Vytvořit fakturu"** jen pro role s `invoices.write` (`POST /jobs/{id}/invoice` přes `InvoiceService.CreateDraftAsync`, idempotence `Job.InvoiceId` → 2. pokus 409/vrátí existující, vyžaduje `Job.ClientId` jinak 422; **technik `Employee` fakturu netvoří**) → přechod do editoru faktury `?id=`, mobil sticky lišta. `NabidkyPage.vue` V2: `QuoteItem.kind` (work|material), server totals, přechody stavů (+`expired` přes `POST /quotes/{id}/status`), „Převést na zakázku" (`POST /quotes/{id}/convert-to-job`, idempotence `sourceQuoteId`). `useJobs`/`useServiceItems`/`useQuotes` mock-capable (CRUD/detail/pracovní list localStorage; materiál+faktura API-real s mock stubem pro e2e). Backend (`vystaveno-api`, jeden PR): entity + migrace `ServicesJobsV2` (forward-only, backfill `quote→scheduled`/`invoiced→done`, `StockMovementType.JobConsumption`/`StornoJobConsumption`, `StockMovement.RelatedJobId`, `QuoteItem.Kind`), controllery service-items/jobs/quotes, `JobStatusStateMachine`/`QuoteStatusStateMachine`, reuse InvoiceService/StockService/SigningEnvelope, integrační testy (`JobWorksheetTests`, `ServiceItemsTests`, `JobsQuotesTests`) vč. tenant isolation + role/location authz + idempotence faktury + odečet/vrácení skladu. **Dokumenty a fotografie V1:** znovupoužitelný `EntityAttachmentsPanel.vue` za checklistem používá API-only `useDocuments` proti `GET/POST /jobs/{jobId}/files`, `GET .../{fileId}/content`, `DELETE .../{fileId}`. Přijímá PDF/JPEG/PNG/WebP do 10 MiB, multiple upload s progressem, metadata, download a potvrzené smazání; `jobs.read` list/download, `jobs.manage` upload/delete. Mock režim ukazuje jen pravdivý nenápadný stav. `http.upload(FormData)` neposílá vlastní `Content-Type` a zachovává 401 refresh/retry. Deploy drží persistentní named volume `api_files` na `/var/lib/vystaveno/files` (`BlobStorage__LocalRootPath`). Ostrý podpis zůstává jen seam. E2E `e2e/services-jobs.spec.ts` + `e2e/job-documents.spec.ts`, demo `src/lib/seed.ts`. Nav „Zakázky" viditelná i pro technika (`Employee`); `Ceník služeb` jen vedení.
- **Opakované faktury V2 — pravidelná fakturace ze šablony (modul `invoicing`):** rozšíření modulu `invoicing`, **žádný nový modul ani permission** (reuse `invoices.read/write`). Kontrakt `docs/backend/recurring-invoices-v2.md`, manuál `docs/product/opakovane-faktury-manual.md`. Firma nastaví šablonu (klient, název, den v měsíci, splatnost, položky NET) a systém ve správný den **bezpečně vytvoří fakturu bez duplicit**. **Bezpečný default = KONCEPT** (`autoIssue=false`); auto-vystavení je opt-in per šablona. Typy `RecurringInvoiceTemplate/Item/Run` + `RecurringInvoiceStatus` (`active|paused`) v `src/lib/types.ts`; čistá logika plánu (`computeNextRunDate`/`advanceRunDate`/`clampToMonth` — **konec měsíce** 31.→28./29./30., `periodKey`) v `src/lib/recurring.ts` (+`recurring.spec.ts`, zrcadlí backend `RecurringInvoiceSchedule`). Composable `useRecurringInvoices` (mock+API, klíč `vystaveno:recurring-invoice-templates`, vzor `useJobs`): `list/get/create/update/remove/pause/resume/generateNow`; **součty a `nextRunDate` počítá VÝHRADNĚ server**, FE je jen zobrazuje. Stránka `RecurringInvoicesPage.vue` na `/app/opakovane-faktury` (`requiresModule: 'invoicing'`, nav `hiddenForRoles: ['Employee']`): desktop tabulka + mobil karty, **jasný stav dalšího běhu**, dialog (klient, den, splatnost, auto-vystavení, položky), **Pozastavit/Obnovit**, **Vygenerovat teď** (→ toast s odkazem na `/app/faktury/editor?id=`), v detailu **odkazy na vytvořené doklady** (`runs`). Backend (`vystaveno-api`, jeden PR): entity + migrace `RecurringInvoicesV2`, `RecurringInvoiceService` (generování VÝHRADNĚ přes `InvoiceService.CreateDraftAsync`+`IssueAsync`), `RecurringInvoiceJob` (background, ambient tenant, idempotence per `periodKey`, row-lock, tenant izolace, audit). Demo `src/lib/seed.ts` (`seedRecurringTemplates`). E2E `e2e/recurring-invoices.spec.ts`. **Vědomě mimo milník:** auto e-mail vygenerované faktury a napojení platby (zůstává ruční); MVP jede měsíčně.
- **Modul `verified_signing` / ověřené podpisy (BankID jako jeden z kanálů):** samostatný placený add-on pro ověřené podepisování dokumentů (smlouvy, předávací protokoly, dohody, objednávky, nabídky, zaměstnanecké dokumenty), NE součást gastro/POS ani fakturačního odeslání. **Frontend foundation je hotová:** stránka `/app/podpisy` (`src/pages/PodpisyPage.vue`) + composable `src/composables/useVerifiedSigning.ts` s provider-neutral typy a metodami proti budoucímu kontraktu (`GET/POST /api/v1/verified-signing/envelopes`, `GET .../{id}`, `POST .../{id}/send`, `POST .../{id}/cancel`, `GET .../{id}/evidence`). Modul `verified_signing` v `src/lib/modules.ts` gatuje nav „Podpisy" i route `/app/podpisy` (zobrazí se jen když app shell umí modul; v mock režimu je default zapnutý). Stránka umí seznam podpisových obálek se stavy `Draft/Ready/Sent/Signed/Rejected/Cancelled/Expired`, filtr stavu, „Nová obálka" a detail (název dokumentu, externí reference, podepisující + e-mail/telefon, provider/kanál, evidence hash, created/sent/signed časy + evidence trail). V mock režimu běží na realistických demo datech. **Nastavení poskytovatelů + credential trezor** je druhý tab stránky (`Obálky` / `Provider podpisů`, `src/components/app/SigningProviderSettings.vue`) — analogie payment credential vault pro modul `verified_signing`: katalog poskytovatelů (`GET /api/v1/verified-signing/providers`, Mock/BankID), CRUD konfigurací (`GET/POST/PUT/DELETE /api/v1/verified-signing/provider-connections`, režim `sandbox/production`, stav `draft/awaiting_credentials/ready/disabled`, checklist NE-tajných setup polí) a **zabezpečený trezor credentialů** (`GET .../provider-connections/{id}/secrets` vrací jen stav polí, `PUT/DELETE .../secrets/{field}` a `DELETE .../secrets` — hodnoty se NIKDY nezobrazí, input se po uložení vyčistí, odebrání povinného klíče degraduje konfiguraci na `awaiting_credentials`). Composable `useVerifiedSigning` má `listSigningProviders`, `list/get/create/update/deleteProviderConnection` a `list/store/delete/revokeConnectionSecret(s)`. Chybové hlášky: 403 = modul/role, 503 = chybí `INTEGRATIONS_SECRET_ENCRYPTION_KEY` na VPS (`Integrations__SecretEncryptionKey` v API), 422 = pole. Provider settings + trezor jsou jen v API režimu (mock/náhled ukáže poznámku); BankID = „Připraveno k napojení", ne ostrý podpis hotový. **Odeslání přes provider connection (runtime seam):** v detailu obálky akce `Odeslat` nabídne (jen API režim) výběr konfigurace stejného providera (přednostně `ready`, ostatní disabled); `useVerifiedSigning.sendEnvelope(id, providerConnectionId?)` pošle volitelné `providerConnectionId` do `POST .../{id}/send`. Bez výběru zůstává základní foundation odeslání jako dřív. Po odeslání detail ukáže `providerReference`, pokud ji backend vrátí. Chybové stavy: 422 „runtime adapter" u BankID → „BankID konfigurace je připravená, ale ostrý adapter ještě není zapnutý.", 422 credentials/Ready nasměruje do `Provider podpisů`, 403 = modul/role. **Backend provider kontrakt staví Codex paralelně**; UI je připravené na `/api/v1/verified-signing/*` bez přepisování. BankID je jen JEDEN z kanálů (provider-neutral), NE jediná technologie. Ostré podepisování zapne až reálný provider adapter + smlouva — do UI ani obchodních textů netvrď konkrétní právní účinek podpisu ani „živé BankID je hotové", dokud není potvrzený provider kontrakt a wording.
- **API a webhooky (Public API V1, modul `integrations`):** stránka `/app/nastaveni/api-webhooky` (`ApiWebhookyPage.vue`, route `requiresModule: 'integrations'`, link karta v `Nastavení firmy → Integrace a exporty`; UI gate `auth.hasRole('Owner','Admin')` — backend vynucuje `integrations.api` vždy; jen API režim, jinak poznámka). Composables `useApiTokens` (list/create/revoke, katalog `API_TOKEN_SCOPES` read-only V1: products/customers/invoices/sales/stock `.read`) a `useWebhooks` (CRUD, `sendTest(id)` → 202 + toast, `deliveries(id, page)` = stránkovaná historie). **Token (`vst_…`) i webhook signing secret (`whsec_…`) přijdou v odpovědi create JEN JEDNOU** — sdílený `SecretRevealDialog.vue` (copy button, explicitní zavření); výpisy nesou jen `tokenPrefix`, secret se už nikdy nevrací. Webhook = HTTPS URL + výběr eventů (`WEBHOOK_EVENTS`; `ping` si nejde předplatit — jen tlačítko Test) + enabled Switch; historie doručení ukazuje stav `Pending/Succeeded/Failed` (terminální po 6 pokusech, backoff 1 min→12 h), HTTP status, zkrácenou chybu a další pokus. Chybové stavy: 403 = role/modul, 503 = chybí `Integrations__SecretEncryptionKey` na VPS, 422 = validace (https-only URL, neznámý event, cap 20 tokenů / 10 webhooků). Kontrakt `docs/backend/api-tokeny-webhooky.md`; integrátorská dokumentace (ověření podpisu HMAC-SHA256, retry, idempotence přes event id) v backend repu `docs/verejne-api.md`. E2E `e2e/api-webhooky.spec.ts` (mockované API routes), unit `useApiTokens.spec.ts`/`useWebhooks.spec.ts`. Zápisové scopes = V2.
- **Stav integrací:** `src/lib/integration-readiness.ts` je frontendový zdroj pravdy pro sekci `Integrace a exporty` v `Nastavení firmy`. Backend #219 přidal foundation endpointy `GET /api/v1/integrations/exports`, `GET /api/v1/integrations/terminal-payments` a `GET /api/v1/integrations/print-jobs` za modulem `integrations`; backend #220 přidal `GET /api/v1/integrations/exports/download` a první reálný účetní adaptér `target=Pohoda&format=Xml`; backend #222 přidává správu lokálních tiskových agentů (`GET/POST/DELETE /api/v1/integrations/print-agents`) a tokenovou gateway pro desktop agenta. Frontend `src/composables/useIntegrations.ts` je mapuje do panelu v nastavení: UI v API režimu se zapnutým modulem ukazuje Generic CSV/Pohoda XML účetní export, poslední/pending terminálové platby, tiskovou frontu a tiskové agenty. Registrace agenta ukáže token jen jednou; výpis token nikdy nevrací; revoke zneplatní lokálního agenta. Sekce `Platební provideri` je **provider-neutral katalog** (`GET /api/v1/integrations/payment-providers/catalog` přes `useIntegrations.listPaymentProviderCatalog()`, typ `PaymentProviderCatalogItem`): karty providerů se stavem `Aktivní` (`isOperational`) / `Připraveno k napojení`, schopnostmi (terminál / online platby / webhooky) a požadavky (smlouva / credentials / setup pole). ČSOB, NFCTRON, Comgate, SumUp i GP webpay jsou **podporované integrační cíle, NE Stripe-first** — ostré platby běží jen přes implementovaný runtime adapter, takže UI nemá žádné tlačítko „zaplatit" ani netvrdí, že plánovaný provider už ostře funguje. Katalog má vlastní loading/error stav (403 = modul/práva). Nad katalogem je **konfigurace napojení**: akce „Nastavit" na kartě otevře dialog (`PaymentProviderConnection`, CRUD `useIntegrations.list/create/update/deletePaymentProviderConnection` proti `/api/v1/integrations/payment-provider-connections`), kde obsluha založí konfiguraci: název, režim `sandbox`/`production`, provozovna, stav `draft`/`awaiting_credentials`/`ready`/`disabled`. Backend #225 dodal skutečný **zabezpečený trezor credentialů** a frontend ho obsluhuje: `useIntegrations` má `listPaymentProviderSecrets` (`GET .../{id}/secrets` — jen stav polí, NIKDY hodnoty), `storePaymentProviderSecret` (`PUT .../secrets/{field}` body `{ value }`), `deletePaymentProviderSecret` a `revokePaymentProviderSecrets` (`DELETE .../secrets/{field}` i `.../secrets`). V dialogu `Nastavit` u uložené konfigurace se zobrazí trezor: per credential pole `required`/`hasSecret`/`updatedAt`, bezpečný password input pro **uložit/rotovat klíč** (po úspěchu se input VŽDY vyčistí, uložená hodnota se nikdy nezobrazí zpět), akce smazat klíč a revokovat všechny. Chybové hlášky: 403 = chybí modul/práva, 503 = chybí serverový šifrovací klíč na VPS (`Integrations__SecretEncryptionKey`), 422 = nekompletní/nevalidní pole. `Ready` vyžaduje kompletní credential set (backend jinak 422); odebrání povinného klíče degraduje konfiguraci na `awaiting_credentials`. Checklist `Potřebné údaje` (`configuredFields`) drží jen NE-tajná setup pole — katalogové `credentialFields` (`*Ref`) patří výhradně do trezoru. Backend vault šifruje AES-GCM server-side; **ostré strhávání plateb zapne až samostatný provider runtime adapter (další milestone)** — uložení klíče platbu nespouští. Mimo API režim zůstává readiness přehled. Pohoda je souborový XML export pro ruční import, ne živá synchronizace. Nepiš do UI ani dokumentace, že existuje reálná terminálová brána (ČSOB/NFCTRON/Comgate/SumUp/GP), Money/SuperFaktura renderer nebo partnerské API, dokud nevznikne skutečný runtime adapter.
- **Veřejný slug firmy:** `Nastavení firmy` ukládá `Company.publicSlug` přes `/company`; frontend ho normalizuje na malá ASCII písmena, čísla a pomlčky. Slug je sdílený základ pro `/objednavka/:slug`, veřejné rezervace a QR odkazy ke stolům.
- **QR objednávka ke stolu:** veřejná route `/objednavka/:slug?table=<tableId>&name=<tableName>` schová výdej/rozvoz, odešle `tableId` přes `usePublicOrders` a backend objednávku připíše do dine-in účtu stolu. Na mobilu `PublicOrderPage` po přidání položek ukazuje sticky spodní košík s počtem položek, totalem a skokem na checkout. E2E `e2e/public-order-table.spec.ts` ověřuje hostovský table-link flow bez JWT.
- **Produktové alergeny:** backend PR #208 přidává `Product.Allergens` jako číselník 1–14 (validace rozsahu a duplicit, normalizace vzestupně) a vrací ho i ve veřejném menu `GET /api/v1/public/{slug}/menu`. Frontend číselník je v `src/lib/allergens.ts`; `Sklad / katalog` ukládá alergeny v product create/update payloadu a `PublicOrderPage.vue` je zobrazuje hostovi u položky menu. Alergeny jsou informační/legal text, neovlivňují cenu, sklad, receptury ani modifikátory.
- **Gastro receptury/BOM:** editor je v `Sklad / katalog` jako akce u produktu (`ProductRecipeDialog.vue`) a volá `GET/PUT/DELETE /api/v1/products/{productId}/recipe`. Backend PR #153 ve `vystaveno-api` zapíná odečet surovin při POS i restaurant payment prodeji: produkt s recepturou odečítá suroviny, produkt bez receptury dál odečítá sám sebe, storno vrací původní skladové pohyby. Řádek receptury má `quantity` (čistá porce), `wastePercent` (odpad/výtěžnost navíc) a `effectiveQuantity` (skladová spotřeba). Dialog receptury počítá živě náklady, prodejní cenu, marži a food cost z effective quantity pro okamžitou kontrolu profitability.

- **Gastro Recipe V2:** `Sklad / katalog` exposes `productKind` (`Standard`/`SemiProduct`), `ProductVariantsDialog.vue` via `useProductVariants` for `GET/PUT/DELETE /products/{id}/variants`, and `ProductionBatchDialog.vue` for `POST /production-batches`. Batch creation uses the server recipe and sends only output quantity/note. `ProductVariantSelectDialog.vue` selects a portion in POS, table service, and public/QR ordering; the client sends only `productVariantId` and displays the server-returned variant name and price. The backend snapshots the variant and recipe consumption, so later catalog changes cannot alter a historical sale or its stock return. API-mode e2e that add a POS or restaurant product must also mock `GET /products/{id}/variants` (`[]` for a base product). Backend architecture: `docs/architecture/gastro-recipe-v2.md` and `docs/architecture/gastro-variant-checkout-snapshots.md`.
- **Gastro modifikátory:** `/app/modifikatory` (modul `stock`) spravuje sdílené skupiny přes `src/composables/useModifierGroups.ts` a backend `GET/POST/PUT/DELETE /api/v1/modifier-groups`. Skupina má typ `Single`/`Multi`, povinnost, volby a příplatky. Přiřazení skupin k produktu řeší `ProductModifiersDialog.vue` v katalogu `Sklad / katalog` přes `GET/PUT /api/v1/products/{productId}/modifier-groups`; PUT body je `{ groups: [{ modifierGroupId, sortOrder }] }`. Výběr při účtování obsluhou je v `ModifierSelectDialog.vue`: `RestauracePage.vue` před přidáním produktu načte/cachuje přiřazené skupiny, vynutí required/single/multi/maxSelect a volá `useOrders.addItem(..., { modifierOptionIds })`. Stejný dialog používá `PublicOrderPage.vue` pro veřejné/QR objednávky: produkt z menu nese `modifierGroups`, košík drží samostatné řádky podle `productId + modifierOptionIds` a `usePublicOrders.order` posílá `modifierOptionIds` v položkách. `OrderItemLine.modifiers`/`KitchenQueueItem.modifiers`/účtenka jsou jen zobrazovací rozpad; cena už je v backendem vrácené `unitPrice`/`lineTotal`, nikdy ji nepřičítat podruhé.
- **Gastro pilot smoke:** `e2e/gastro-pilot-smoke.spec.ts` je rychlá end-to-end brána přes otevřený účet u stolu, kuchyňský bon s modifikátory, platbu kartou a nenulovou otevřenou `Uzávěrku` včetně DPH a prodaného produktu. Spouštěj ji po změnách v `Restaurace`, `Kuchyně`, `Uzávěrka`, `useOrders`, `useKitchen`, `useSalesReport` nebo v gastro platebním toku.
- **Skladové zrcadlo:** backend PR #154 přidává `GET /api/v1/inventory/stock-mirror`; frontend panel je třetí tab `Zrcadlo` na stránce `Zásoby`. UI musí jasně ukazovat `Stav má být`, `Realita` a `Rozdíl`; rozdíl se skládá z korekcí a inventur. Detail řádku vysvětluje vzorec otevření + příjem + storno - prodej - výdej = stav má být a korekce + inventura = rozdíl. Backend PR #157 přidává `unitCost` a `varianceValue`; UI zobrazuje rozdíl v kusech i Kč. Backend PR #158 mění `unitCost` na vážený průměr nákupních cen z příjemek do konce období s fallbackem na nákupní cenu produktu. Zrcadlo v `Zásoby` posílá filtry `from`, `to`, `locationId` a `search`, aby šlo kontrolovat konkrétní den/pobočku/bar. Ruční výdej v `Zásoby` posílá `type` (`Issue`, `WriteOff`, `StaffMeal`, `Breakage`, `Expiration`) a historie ukazuje český důvod pohybu. Přesunový dialog v `Zásoby` volá `POST /inventory/transfers`, backend zapisuje `TransferOut` + `TransferIn`; celkový stav firmy se nemění, ale zrcadlo filtrované podle pobočky ukazuje odchozí/příchozí množství. Backend location-stock foundation ukládá `stock_levels` per `locationId`; frontend `Zásoby` má výběr `Pobočka skladu`, `GET /inventory/stock-levels?locationId=` používá pro pobočkové stavy a příjem/výdej/korekce/inventura posílají `locationId`. Pokud má firma víc poboček a je vybráno `Všechny pobočky`, ruční skladová akce se zablokuje a obsluha musí vybrat konkrétní pobočku. E2E `e2e/stock-mirror-smoke.spec.ts` hlídá slovník `Stav má být`/`Realita`/`Rozdíl`, filtrování a vysvětlení rozdílu v detailu; spouštěj ho po změnách v `Zásoby`, `useInventory`, zrcadle nebo inventuře.
- **Inventura:** dialog v `Zásoby` používá stejný slovník jako zrcadlo: `Stav má být` = systémový stav před uložením, `Realita` = fyzicky napočítáno, `Rozdíl` = realita minus systém. Ukládá se celá inventura přes `/inventory/stocktake`; hledání v dialogu jen filtruje pohled.
- **Nákupní příjemky:** backend PR #156 přidává `POST/GET /api/v1/inventory/purchase-receipts` jako atomický skladový doklad. Frontend `Naskladnění` ukládá hlavičku příjemky, řádky s množstvím a volitelnou nákupní cenou; sklad se mění přes příjemku, ne přes anonymní ruční příjem. `Naskladnění` je pobočkové (stejný vzor jako `Zásoby`): u víc poboček má výběr `Pobočka skladu`, stav/doporučení/poslední příjemky se filtrují přes `locationId` a nová příjemka se ukládá na konkrétní pobočku (`Všechny pobočky` uložení zablokuje). Backend `GET /api/v1/inventory/purchase-receipts` dostal volitelný `locationId` filtr (unrestricted člen bez něj vidí celou firmu, scoped člen jen svou pobočku, cizí → 403); sdílený read-filtr `LocationScope.ResolveForReadAsync` je společný se skladem.
- **Nákupní doporučení:** backend `GET /api/v1/inventory/purchase-suggestions` doporučuje doobjednání podle aktuálního skladu, minim, skutečné spotřeby a receptur. Frontend `Naskladnění` tím plní blok `K doobjednání`, ukazuje doporučené množství, denní spotřebu, dny zásoby a odhad ceny a položku umí rovnou přidat na příjemku; při nedostupném endpointu stránka nespadne a použije starý minimový fallback.
- **Import katalogu z konkurenční pokladny:** `src/import/adapters/pos-products.ts` registruje frontend-only produktové adaptery pro Dotykačka, Storyous/Teya a iKelp CSV/XLSX exporty. Scope je jen katalog/menu: název, PLU/SKU, EAN, prodejní cena, DPH, nákupní cena, kategorie a množství skladem. Commit jde přes existující import pipeline, produktový CRUD, kategorii a příjemku; žádný nový backend import endpoint není potřeba. Neimportuje mapy stolů, modifikátory, alergeny, cenové hladiny ani live POS sync.
- **Import historických tržeb z konkurenční pokladny:** `/app/import?entity=sales` má frontend základ pro CSV/XLSX účtenky z Dotykačky, Storyous/Teya, iKelpu nebo obecného POS. `src/lib/sales-import.ts` seskupuje řádky podle čísla účtenky, normalizuje platbu, datum, DPH a totály; `src/composables/useSalesImport.ts` volá `POST /api/v1/sales/import` s `{ source, dryRun, sales }`. Historické tržby nesmí odečítat sklad ani zpětně měnit Z-reporty; slouží pro analytiku/reporting po přechodu zákazníka z konkurence. Backend kontrakt a rozhodnutí jsou v `docs/backend/import-historie-prodeju.md`.
- **Pobočkové skladové stavy:** backend větev `feat/location-stock-balances` mění `StockLevel` na per-location stav a přidává `locationId` filtr pro `GET /api/v1/inventory/stock-levels`, detail stavu produktu a `low-stock`. Frontend má výběr pobočky v `Zásoby` (stav, low-stock, ruční skladové operace) i v `Naskladnění` (příjemky, doporučení); bez filtru API vrací celofiremní součet.
- **Centrální sklad (přehled napříč pobočkami):** backend `GET /api/v1/inventory/stock-by-location` (read-only, `Inventory.Read`, bez migrace) vrací matici produkt × pobočka: `{ locations: [{locationId, locationName}], products: PagedResult<{productId, productName, productSku, totalQuantity, cells: [{locationId, quantity}]}> }`. Buňky jsou řídké (jen nenulové), `locationId=null` = sloupec „Nezařazeno", scope přes `LocationScope.ResolveForReadAsync` (scoped člen jen svou pobočku). Frontend má v `Zásoby` tab `Podle poboček` (jen při >1 pobočce) — matice v `overflow-x-auto`, lazy load přes `useInventory.stockByLocation`, řídké buňky doplní 0 podle hlavičky sloupců.
- **Uzávěrka:** zavřený den na stránce `Uzávěrka` nabízí běžný čitelný CSV export Z-reportu i účetní CSV export (`src/lib/day-close-export.ts`) se stabilními sloupci pro DPH, platby, spropitné, slevy/storna, prodané produkty a hotovostní rozdíl. Hotovostní uzávěrka posílá na backend `cashOpening`, `cashPayIns`, `cashPayOuts`, `cashCountedClosing`, `cashDrop`; backend počítá očekávanou hotovost jako počátek + hotovostní tržby + vklady - výběry - odvod. Backend odmítne zavření dne s otevřenými gastro účty na provozovně (409); `useDayClose` nechává backendovou hlášku projít do toastu, aby obsluha věděla, že má účty doplatit nebo zrušit. `Provozní předávka` na otevřeném i zavřeném dni ukazuje hotovost/kartu/storna a checklist; `Export předávky CSV` přidává řádky `Předal`, `Převzal`, `Poznámka` pro směnové předání bez nové účetní entity. Tlačítko `Export měsíc účetní CSV` používá backend `GET /api/v1/day-close?from=&to=&locationId=` a stáhne všechny uzavřené Z-reporty z vybraného měsíce/provozovny do jednoho účetního souboru; `Export měsíc souhrn CSV` k nim přidává kontrolní jeden-řádek-na-Z-report souhrn se součtovým řádkem `CELKEM`.
- **Restaurace / účty na stole:** `src/pages/RestauracePage.vue` řídí mapu stolů a otevřené účty přes `src/composables/useOrders.ts` (bez Pinia store — zdroj pravdy je backend, po akci se volá `refreshOpen()`). Sloučení účtů (backend PR #165) volá `POST /orders/{targetOrderId}/merge` s `{ sourceOrderId }`: položky ze zdrojového účtu se přesunou do cílového (`currentOrder`), zdroj backend nastaví na `Cancelled` a jeho stůl se uvolní, sleva/spropitné zůstávají na cíli a split (rozdělení účtu) se resetuje. Částečná platba osoby ze split dialogu volá backend PR #168 `POST /orders/{id}/pay-items` přes `ordersApi.payItems(orderId, paymentMethod, [{ itemId, quantity }])`; backend vytvoří normální `Sale`, odečte sklad/receptury jen pro zaplacená množství, na účtu nechá zbytek a split resetuje. Frontend po této platbě zavře split dialog, obnoví open orders a pro zbytek účtu musí obsluha split nastavit znovu. Otevřený účet se drží aktuální vůči QR doobjednávkám hosta: `RestauracePage.vue` má `refreshCurrentOrder()`, který běží v 5s intervalu (jen API mód; přeskočí při probíhající akci, otevřeném platebním/split dialogu nebo přepnutém stole) a navíc se forced refresh volá v `openPayment()` před platbou celého účtu — obsluha tak platí a tiskne účtenku podle skutečného stavu, ne podle zastaralého snapshotu. Lokální sleva/spropitné (`accountDiscountPercent`/`tipAmount`) se při refreshi nepřepisují; když účet mezitím uzavře jiný terminál (`status !== Open`), refresh vrátí obsluhu na mapu. Souběžné akce, které backend odmítne s 404/409 protože účet už není otevřený, řeší sdílený helper `handleAccountClosedElsewhere` (hláška „Účet mezitím zaplatil nebo zrušil jiný terminál." + `refreshOpen()` + návrat na mapu) — použitý u `addProduct` a `sendToKitchen` (kde 409 znamená jen zavřený účet); pay/split/merge mají tuto logiku vlastní. `changeQty`/`saveNote` ho nepoužívají, protože jejich 409 může znamenat i „položka už byla odeslána do kuchyně".
- **Mobile/tablet waiter flow:** `Restaurace` má na malých a tabletových displejích kompaktní seznam stolů (kvůli stolům mimo absolutně pozicovanou mapu) a při otevřeném účtu sticky spodní lištu s názvem stolu, počtem položek, celkem a akcemi `Odeslat`/`Zaplatit`. Vizuální mapa zůstává od `lg` výš. Při dalších úpravách hlídej, aby hlavní akce obsluhy zůstaly dosažitelné bez dlouhého scrollování.
- **Restaurant POS cockpit:** `/app/restaurace` používá route-scoped `meta.operational` shell bez běžného sidebaru. Široký režim je třípanelový (kategorie/katalog/pevný účet), mobil má horizontální kategorie, seznam stolů, vysouvací účet a sticky primary actions s `checkoutTotal`. Touch targets jsou min. 48 px, hledání filtruje název/SKU/EAN společně s kategorií, polling neběží na skryté kartě ani souběžně a payment vždy dělá forced refresh. Opožděný order polling se zahazuje pomocí refresh/mutation sekvencí, aby nepřepsal novější akci obsluhy. Pořadí výdeje používá `Předkrm` / `Hlavní chod` / `Dezert`; `src/lib/order-courses.ts` mapuje legacy `1./2./3. chod` a tvoří oddělené skupiny na účtu, KDS i tisku. Cancel vyžaduje potvrzení, merge je dvoukrokový. API a `useOrders.ts` se nemění; regresi kryjí `restaurace-mobile.spec.ts` a `restaurace-cockpit.spec.ts`.
- **Kuchyně / KDS:** `src/pages/KuchynePage.vue` čte `GET /api/v1/kitchen/queue?section=` přes `useKitchen`, seskupuje položky do bonů podle účtu + `kitchenSection` a uvnitř je řadí podle chodů pod textové oddělovače, které přebírá i tisk. Umí filtry Vše/Kuchyně/Bar, tisk bonu a workflow `Sent → Preparing → Ready → Served`. Bon ukazuje přípravný čas se SLA barvami (bar přísnější než kuchyně); veřejné objednávky bez stolu zobrazuje podle `customerName` + `fulfillment` (`pickup`/`delivery`) a QR objednávky ke stolu podle `tableName`. Přepínač `Živé / Historie` používá backend PR #172 `GET /api/v1/kitchen/history?section=`; historie je read-only, řadí vydané (`Served`) bony nejnovější napřed podle `kitchenStatusUpdatedAt`, ukazuje odesláno/vydáno/dobu přípravy a nepolluje síť každých 5 sekund jako živá fronta.
- **Pokladna / rychlé přidání:** `src/pages/PokladnaPage.vue` má pole `Sken / EAN` pro HW/Bluetooth čtečku v režimu klávesnice i tlačítko `Kamera`, které používá sdílený `CameraScanner.vue` s vlastní POS hláškou. Přidání hledá přes exact `Product.ean` (`findByEan`); neznámý EAN a duplicitní EAN zobrazují toast a nepřidávají položku. Vedle toho `Hledat produkt` filtruje dlaždice podle názvu, SKU nebo EAN a kombinuje se s kategorií. E2E `e2e/pokladna-scan.spec.ts` hlídá přidání skenem, navýšení množství opakovaným skenem a hledání podle názvu/SKU/EAN.
- **Veřejné objednávky:** route `/objednavka/:slug` (`PublicOrderPage.vue`) používá `usePublicOrders` a public endpointy `GET /public/{slug}/menu` + `POST /public/{slug}/orders` bez JWT. Bez query `table` zákazník vybírá menu do košíku, zvolí `pickup`/`delivery`, vyplní kontakt a objednávka jde rovnou do KDS; ceny vždy počítá backend z katalogu. Produkty s `modifierGroups` otevřou `ModifierSelectDialog`, povinné volby nejdou přeskočit a payload řádku nese `modifierOptionIds`. QR objednávka ke stolu používá `/objednavka/:slug?table=<tableId>&name=<tableName>`, schová volbu výdej/rozvoz, odešle `tableId` a backend objednávku uloží do dine-in účtu stolu; pokud už účet existuje, přidá do něj nové položky. `Mapa stolů` generuje pro vybraný stůl QR kód a kopírovatelný odkaz z `Company.publicSlug` vraceného `/company`.
- **Audit POS/gastro akcí:** backend PR #169 zapisuje append-only audit přes `GET /api/v1/company/audit` pro storno prodeje, změnu slevy/spropitného na otevřeném gastro účtu a uzavření dne/Z-report; backend PR #170 přidává změny prodejní/nákupní ceny a DPH produktu; backend PR #173 přidává query filtr `action=...`. Frontend stránka `src/pages/AuditPage.vue` (`/app/audit`, modul `core`, role Owner/Admin) čte stránkovaný audit přes `useAudit`, zobrazuje české názvy akcí, aktéra, čas, entitu a ne-citlivé detaily z `dataJson` a umí rychle filtrovat klíčové provozní akce.
- **Schvalování rizikových akcí:** backend větev `feat/approval-workflow` přidává `GET/PUT /api/v1/approvals/settings`, `GET /api/v1/approvals`, `POST /api/v1/approvals/{id}/approve|reject` a `202 Accepted` s `ApprovalRequestResponse` pro storno prodeje, ruční výdej/odpis skladu a inventuru nad limitem. Frontend stránka `src/pages/SchvalovaniPage.vue` (`/app/schvalovani`, modul `core`, role Owner/Admin/Manager) spravuje limity a pending/approved/rejected frontu. `useSales.storno`, `useInventory.issue` a `useInventory.stocktake` vrací buď původní výsledek, nebo `ApprovalRequest`; stránky používají `isApprovalRequest` a místo chyby zobrazí toast `čeká na schválení managerem`. E2E `e2e/schvalovani.spec.ts` hlídá manažerskou stranu (nastavení limitů, frontu žádostí a approve/reject workflow) a `e2e/approval-requests-smoke.spec.ts` stranu obsluhy (202 z POS storna, skladového výdeje a inventury zobrazí toast `čeká na schválení managerem`, ověří odeslaný payload a u storna i to, že se akce neprovede a prodej zůstává aktivní). Spouštěj je po změnách ve schvalování, stornech, skladových odpisech nebo inventuře.
- **Cenové hladiny, akce a věrnost:** backend `vystaveno-api` má persistentní CRUD `GET/POST/PUT/DELETE /api/v1/price-levels`, `GET/POST/PUT/DELETE /api/v1/promotions`, serverový výpočet `POST /api/v1/promotions/calculate` a věrnostní kontrakt `GET/POST/PUT /api/v1/customers`, `GET/POST /api/v1/customers/{id}/loyalty...`, `GET/PUT /api/v1/loyalty/settings` (modul `loyalty`, oprávnění `loyalty.read/manage`). Backend PR #216 napojuje automatická promo pravidla do ostrého checkoutu přes sdílený `SaleService.PersistSaleAsync`; backend PR #217 přidává volitelný `PriceLevelId` pro POS i platbu účtu; backend PR #218 přidává zákazníky, ledger bodů, earn/redeem a snapshot `customerId`, `redeemPoints`, `redeemDiscount`, `earnedPoints` na účtence. Frontend `src/composables/usePromotions.ts`, `src/composables/useCustomers.ts` a `src/composables/useLoyalty.ts` mapují backend kontrakty. `src/pages/AkceProvozuPage.vue` (`/app/akce-ceny`) spravuje hladiny/pravidla; `src/pages/VernostPage.vue` (`/app/vernost`) spravuje nastavení programu, zákazníky, ledger a ruční úpravy bodů. `PokladnaPage.vue` a `RestauracePage.vue` načítají cenové hladiny i zákazníky, před platbou volají serverový náhled `/promotions/calculate`, v souhrnu ukazují dopad hladiny/akcí/body, posílají `priceLevelId`, `customerId` a `redeemPoints` přes `useSales.create` nebo plnou platbu `useOrders.pay` a účtenka ukazuje uplatněné/získané body. Split platba osoby používá backend `pay-items`, který zatím `customerId/redeemPoints` nemá, takže věrnost se tam neuplatňuje. `src/lib/promotions.ts` zůstává mock/fallback a čistý sdílený model.
- **Provozní přehled (manažerský dashboard):** `src/pages/ProvozniPrehledPage.vue` (`/app/provozni-prehled`, modul `reporting`, role Owner/Admin/Manager) čte manažerskou analytiku prodejů přes `src/composables/usePosReports.ts` z backendu `GET /api/v1/pos-reports/summary`, `/revenue`, `/costs`, `/staff`, `/losses` a `/dead-items`. Typy + čistý helper `posReportRange` (rozsahy z lokálního data) jsou v `src/lib/posReports.ts`. Ukazuje KPI tržeb a plateb, graf tržeb po dnech (`BarChart`), nejprodávanější položky, blok `Marže a food cost` s hrubou marží, food cost %, odhadovaným nákladem, tržbami mimo katalog a produkty s nejvyšším food costem, blok `Výkon obsluhy` s tržbami/účty/průměrem/platbami/tipy/slevami/storny, procentem slev a procentem storen po zaměstnanci včetně `Nepřiřazeno`, blok `Ztráty skladu` s provozními ztrátami, inventurními ztrátami, nálezy, důvody a největšími ztrátovými položkami v Kč a blok `Ležáky skladu` s kladným skladem bez prodeje v období, známou skladovou hodnotou, položkami bez nákladové ceny a posledním prodejem; filtr období a provozovny. Jen API režim. Backend (oprávnění `pos.reports`, Owner/Admin/Manager) agreguje jen dokončené prodeje konzistentně s uzávěrkou dne; `/costs`, `/losses` a `/dead-items` používají receptury/příjemky/nákupní ceny jako MVP odhad nákladu.
- **Konsolidace poboček:** `src/pages/KonsolidacePage.vue` používá starší `useApi<Sale>('sales')` pro tržby po pobočkách a v API režimu navíc volá `usePosReports` pro každou aktivní pobočku. Čistá logika je v `src/lib/consolidation.ts`: `consolidationReportRange` mapuje zvolené období (`all` nebo `YYYY-MM`) na reportovací rozsah a `buildLocationOperationalComparison` skládá pobočkový řádek z `/summary`, `/costs`, `/losses` a `/dead-items`. UI ukazuje gross margin, food cost, stock losses, dead stock value a margin after losses, s CSV exportem `provozni-srovnani-pobocek.csv`. V mock režimu zůstává jen tržbová konsolidace bez serverových provozních reportů.
- **Git:** uživatel dal souhlas průběžně commitovat a pushovat dokončené změny. Stále pracuj na feature větvích a přes PR do `main`; nepushuj přímo do `main`, pokud si to výslovně neřekne.

## 0.1 Kontext mezi Codexem a Claude Code

**AGENTS.md je hlavní společný kontext pro AI agenty. Tento CLAUDE.md je Claude Code vstup a musí s AGENTS.md zůstat v souladu.**

Při každé smysluplné změně projektu aktualizuj **AGENTS.md i CLAUDE.md**. Platí to hlavně pro architekturu, doménový model, modulový systém, API kontrakty, deploy postup, bezpečnostní pravidla, workflow vývoje, testovací brány a důležitá produktová rozhodnutí.

Když se pravidlo týká všech agentů, napiš ho do AGENTS.md a zrcadli/odkaž ho tady. Když je specifické jen pro Claude Code, může být jen tady, ale nesmí odporovat AGENTS.md.

## 1. Nejdřív přemýšlej, pak piš kód

Nepředpokládej. Neskrývej zmatek. Pojmenuj tradeoffy.

Před implementací:

- Své předpoklady řekni nahlas. Pokud si nejsi jistý, zeptej se.
- Pokud existuje víc možných výkladů, ukaž je — nevybírej potichu.
- Pokud existuje jednodušší cesta, řekni to. Když to dává smysl, oponuj.
- Pokud je něco nejasné, zastav se. Pojmenuj, co tě mate. Zeptej se.

## 2. Nejdřív jednoduchost

Minimum kódu, který řeší daný problém. Nic spekulativního.

- Žádné featury navíc, které nebyly zadány.
- Žádné abstrakce pro kód použitý jen jednou.
- Žádná „flexibilita" nebo „konfigurovatelnost", o kterou nikdo nepožádal.
- Žádné ošetření chyb pro scénáře, které nemůžou nastat.
- Pokud jsi napsal 200 řádek a šlo to na 50, přepiš to.

Zeptej se sám sebe: „Řekl by senior engineer, že je to zbytečně složité?" Pokud ano, zjednoduš.

## 3. Zásahy, opravy a updaty

Sahej jen na to, na co musíš. Uklízej jen po sobě.

Když upravuješ existující kód:

- „Nevylepšuj" sousední kód, komentáře ani formátování.
- Nerefaktoruj věci, které nejsou rozbité.
- Drž se stávajícího stylu, i kdybys to dělal jinak.
- Pokud si všimneš nesouvisejícího mrtvého kódu, zmiň ho — nemaž ho.

Když tvé změny vytvoří „osiřelý" kód:

- Smaž importy / proměnné / funkce, které tvé změny udělaly nepoužívanými.
- Nemaž mrtvý kód, který tu byl už předtím, pokud o to nikdo nepožádá.

Test: Každý změněný řádek by měl jít přímo dohledat k požadavku uživatele.

## 4. Exekuce podle cíle

Definuj kritéria úspěchu. Cyklicky ověřuj, dokud nesedí.

Z úkolů udělej ověřitelné cíle. V tomhle projektu (frontend) ověření typicky znamená:

- **Build projde čistě:** `npm run build` (včetně typecheck) bez chyb.
- **Lint projde:** `npm run lint` bez chyb.
- **Testy projdou:** `npm run test` (vitest unit) a u UI flows `npm run test:e2e` (Playwright).
- **Vypadá to správně:** stránka naběhne v prohlížeči bez chyb v konzoli.
- API e2e flow může před načtením appky nastavit `window.__VYSTAVENO_API_URL__ = '/api/v1'`; produkce dál používá build-time `VITE_API_URL`.

Když task výslovně přidává testy, pak platí: „napiš test, který stav reprodukuje, a doveď ho k zelené."

U vícekrokových úkolů sepiš krátký plán:

```
1. [Krok] → ověření: [kontrola]
2. [Krok] → ověření: [kontrola]
3. [Krok] → ověření: [kontrola]
```

Silná kritéria úspěchu ti umožní pracovat samostatně v cyklu. Slabá kritéria („udělej to, ať to funguje") vyžadují neustálé doptávání.

## 5. Multi-agent pipeline (orchestrace)

U netriviálních úkolů orchestruje hlavní smyčka tyto subagenty (žijí v `.claude/agents/`, lokálně/gitignorováno): `solution-architect`, `qa-tester`, `invoice-code-reviewer`, `debugger`, `a11y-specialist` a `orchestrator` (plánovač). Subagent neumí spustit jiného subagenta — řetězení a předávání kontextu řídí hlavní smyčka.

Default routing podle typu tasku:

- **Nový feature** → `solution-architect` (návrh) → implementace → `qa-tester` → `invoice-code-reviewer`
- **Bug fix** → `debugger` (root cause) → oprava → `qa-tester` → `invoice-code-reviewer`
- **Refactor** → `invoice-code-reviewer` (stav) → `solution-architect` → implementace → `qa-tester`
- **A11y / přístupnost** → `a11y-specialist` (kontrast, focus, ARIA, dark mode) → `qa-tester` (axe e2e regrese)
- **Code review / architektura** → příslušný agent samostatně
- **Nejasný task** → upřesnit PŘED spuštěním pipeline

Pravidla:

- Triviální úkoly (drobná oprava, přejmenování, copy, dep bump) pipeline NEspouštějí — selský rozum (viz tradeoff nahoře).
- Mezi kroky předávej relevantní kontext (nálezy předchozího agenta dalšímu).
- Po pipeline agreguj výstupy agentů a uveď finální doporučení + stav ověření (build / lint / testy).

---

Tato pravidla fungují, když: v diffech je méně zbytečných změn, méně přepisování kvůli překomplikování, a doptávání přichází před implementací, ne až po chybách.
